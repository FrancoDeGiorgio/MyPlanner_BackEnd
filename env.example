# ============================================================================
# env.example - Template per variabili d'ambiente MyPlanner Backend
# ============================================================================
# Copia questo file come .env e compila i valori reali
# MAI committare il file .env con valori reali!

# ============================================================================
# DATABASE
# ============================================================================
# Stringa di connessione PostgreSQL
# Formato: postgresql://[user]:[password]@[host]:[port]/[database]
DATABASE_URL=postgresql://myplanner:CHANGE_ME@localhost:5432/myplanner

# ============================================================================
# JWT SECURITY (CRITICO!)
# ============================================================================
# SECRET_KEY è OBBLIGATORIA e deve essere almeno 32 caratteri
# Genera una chiave sicura con:
#   python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=GENERATE_32_CHAR_RANDOM_STRING_HERE

# Algoritmo di firma JWT (default: HS256)
# ALGORITHM=HS256

# Durata validità access token in minuti (default: 30)
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Durata validità refresh token in giorni (default: 7)
REFRESH_TOKEN_EXPIRE_DAYS=7

# JWT Issuer (default: myplanner-api)
JWT_ISSUER=myplanner-api

# JWT Audience (default: myplanner-users)
JWT_AUDIENCE=myplanner-users

# ============================================================================
# APPLICATION CONFIGURATION
# ============================================================================
# Nome applicazione (default: MyPlanner API)
APP_NAME=MyPlanner API

# Versione applicazione (default: 2.0.0)
APP_VERSION=2.0.0

# Ambiente di esecuzione: development, staging, production
ENVIRONMENT=development

# Debug mode (disabilitare in produzione!)
# Valori: true, false, 1, 0, yes, no
DEBUG=false

# Colore accent di default per nuovi utenti (default: #7A5BFF)
DEFAULT_ACCENT_COLOR=#7A5BFF

# ============================================================================
# DOCKER / DEPLOYMENT
# ============================================================================
# Se presente, indica che l'app è in esecuzione su Render
# Questo disabilita il caricamento da .env (usa variabili d'ambiente)
# RENDER=true

# ============================================================================
# NOTE SICUREZZA
# ============================================================================
# - SECRET_KEY deve essere UNICA per ogni ambiente (dev/staging/prod)
# - Non condividere mai SECRET_KEY tra ambienti diversi
# - In produzione, usa un secret manager (AWS Secrets Manager, HashiCorp Vault, etc.)
# - Rotazione periodica di SECRET_KEY richiede re-login di tutti gli utenti
# - DATABASE_URL contiene credenziali: proteggere con cura
# - Non committare mai file .env con valori reali!

